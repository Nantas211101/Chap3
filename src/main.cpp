#include <iostream>
#include "../include/SceneNode.hpp"

int main()
{
    // sf::RenderWindow window(...);
    // SceneNode::Ptr node(...);
    // window.draw(*node); // note: no node->draw(window) here!
    return 0;
}